#include <stdio.h>

int main() {

  // 59.
  /*
  int num;
  int *ptr = &num;
  printf("Welcome to showcasing integer pointers.\n");
  printf("Please enter the values: ");
  scanf("%d", ptr);
  printf("%d is the adress of pointer\n", ptr);
  printf("%d is the actual number\n",*ptr);
  printf("%d is the value of number", num);
  */

  // 60.
  /*
  printf("Chaning value of a integer variable using pointer\n");
  int z = 59;
  int *ptr =&z;
  printf("%d is the real number\n", z);
  *ptr = 58;
  printf("%d after changing the real number\n", *ptr);
  */

  // 61
  /*
  char c;
  char *ptr = &c;
  printf("Welcome to showcasing character pointers\n");
  printf("Enter the character: ");
  scanf("%c",&c);
  printf("%d is the adress of pointer\n", ptr);
  printf("%c is the actual charachter\n",*ptr);
  printf("%c is the value of character", c);
  printf("%d is the Ascii actual character\n",*ptr);
  printf("%d is the Ascii value of character", c);
  */

  // 62
  

  return 0;
}